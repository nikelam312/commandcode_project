[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "crawler-common"
version = "0.1.0"
description = "Shared proxy/config/fetch/db/alerts modules for the crawler workspace"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["crawler_common"]